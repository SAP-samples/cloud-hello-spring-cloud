applications:
- name: hello-spring-cloud-${random-word}
  memory: 512M
  instances: 1
  host: hello-spring-cloud
  path: target/ROOT.war
  services:
    - hana
  env:
    SPRING_PROFILES_DEFAULT: cloud
